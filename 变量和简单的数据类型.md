
## 1. 变量

1.1 变量是Python里可以随时修改值的量，==是可以被赋值的标签==，Python将始终记录变量的最新值。每个变量都指向一个值与该变量相关联的信息。

[[C语言概述#^cc64e6]](与C中的标识符类似)
1.2 变量的命名和使用：
1.2.1命名：
1. 变量名只能包含字母，数字和下划线。变量名能以字母和下划线开头，但不能以数字开头。
2. 变量名不能包含空格，但能使用下划线来分隔其中的单词。
3. 不要将关键字和函数名用作变量名，前者报错，后者虽能运行但会导致函数无法使用。
4. 变量名最好由简洁又具有描述性。
5. 慎用l,O这样的字母，可能被当成0，1。

## 2. 字符串

2.1：字符串就是一系列字符。再Python中用引号引起来的都是字符串，其中的引号可以是单引号也可以是双引号。这样的设计可以使你灵活的使用撇号和引号。但要注意不能单引号套单引号，双引号套双引号。

2.2 使用==方法==修改字符串的大小写：
    方法（*method*）是Python可对数据执行的操作。每个方法后面都有一个括号，因为方法通常需要额外的信息才能完成工作，有的可以不用，所以为空阔号
   name = "ada"
   1. name.title() --字母变大写
   2. name.upper() --字母变大写
   3. name.lower() --字母变小写
   另外这些操作无法更新变量，若想更新，需要对变量进行赋值：name = name.title()

2.3 在字符串中使用变量：

   first_name = "ada"
   last_name = "love"
   full_name = f"{first_name} {last_name}"

在这个例子中，f是format（设置格式）的简写，Python通过把花括号内的变量替换为其值。

2.4 使用换行符或制表符来添加空白：

1. /n 可以将打印的内容换行
2. /t  可以在打印内容前先打出4个空格
3. /n/t可以叠加使用来设置打印的效果。

2.5 删除空白方法：

  1. .rstrip() --删除右侧空白
  2. .lstrip() --删除左侧空白
  3. .strip()  --删除两端空白

2.6 删除前后缀的方法：

  1. removeperfix('要删除的前缀')
  2. removesuffix('要删除的后缀')

## 3 数

Python根据数的不同的用法来处理它们

3.1 整数：

  1. 可以对整数进行“+ - * /  ** ”，加减乘除，乘方或其他的科学运算运算。
  2. 空格不影响python表达式的方式。

3.2 浮点数：浮点数（又小数点）可以和整数一样做运算，但结果包含的小数位可能是不确定的，这个不用担心。

3.3 整数和浮点数：

  1. 将任意两个数相除，结果总是浮点数，即便这两个数都是整数且能整除。
  2. 在其他任何运算中，如果一个操作数是整数，另一个是浮点数，结果也总是浮点数。

3.4 数中的下划线：在写很大的数字时可以用下划线分为，python不会把下划线记录进数据中，也不会打印出来，仅仅方便你阅读。

3.5 同时给多个变量赋值：

  x,y,z = 0,0,0

对变量和值都要用逗号隔开，python会按照顺序为这些变量赋值。要保证变量和值的数量是相等的。

